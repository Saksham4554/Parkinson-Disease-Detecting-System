# Parkinson Disease Prediction 

This project predicts Parkinson’s disease using Machine Learning (SVM).

## How to Run

pip install -r requirements.txt
streamlit run app.py

## Files
- app.py → main app
- model.joblib → trained model
- scaler.joblib → scaler
